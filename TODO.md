# TODO LIST

- [x] **[MEDIUM]** Add responsive breakpoints so fixed-width containers reflow on tablet and mobile
  - Type: bug
  - Description: The layout uses fixed pixel widths/heights with no @media queries anywhere in style.css, causing overflow/clipping on narrow viewports: #main's 1000px center column, #spaceForm's 450px search input column, and #outerContainer's mixed px/vh row heights. Add a ~768px tablet breakpoint that collapses #main to a single "weatherOuter" column (hiding #sideSpace1/#sideSpace2) and shrinks #spaceForm's grid-template-columns to a smaller fixed input width, and a ~480px mobile breakpoint that stacks #weatherOuter's inner1/innerMain/inner2 into a single column and makes #spaceForm a full-width single-column stack (formInput above formButton) with adjusted padding/gap. Likely code location is the CSS rules for #outerContainer, #main, #weatherOuter, and #spaceForm.
  - File: `WeatherApp_main/src/style.css`
  - Completed: 2026-07-15
  <!-- id: 3b17b856-b659-446f-9696-86e5f1f54fd0 -->

- [x] **[MEDIUM]** Add per-weather page theming and complete the 768/480 breakpoint reflow
  - Type: feature
  - Description: Two bundled changes. (1) Theming: `body` and `#outerContainer` have no `background-color`, so the white `#weatherUpper`, `#weatherData`, `#dateMain` and `#weatherType` surfaces currently sit invisibly on browser white. Give `#outerContainer` a per-weather vertical sky gradient and carry a matching accent onto the two existing underline rules (`#dateMain`, `#weatherType`), `#footer` and `#formButton`; cards stay white. Drive it by setting a `weather-{rain|snow|hail|partly|sunny|fog|overcast}` class on `#outerContainer` inside the existing `adjustWeather()` branches at `indexChanges.js:76-119`, which makes the theme follow the ‹ › day navigation for free via `day2Call`/`day3Call`; add a neutral fallback class for conditions that match nothing ("Cloudy", "Mist", "Clear") and a `transition: background 400ms` cross-fade. All colors live in `style.css` — no inline style writes, no new deps. Two blockers to clear first: the seven regexes have no `i` flag while `json.day.condition.text` comes back Title Case ("Sunny", "Partly cloudy", "Overcast", "Fog"), so only `rain` and `snow` currently match mid-string and four icon branches are already dead — add `i` or the theme stays silent on most days; and the `formButton` mouseleave handler in `index.js` hardcodes `this.style.background = "#ccc"`, an inline style that will override the themed accent on first hover, so move the `formButton` and arrow hover treatments out of `index.js` into CSS `:hover`. Keep the icons as `<img src>` (CSS can't reach inside them to recolor), which is why surfaces stay light rather than shifting per weather. (2) Breakpoints: the shipped 768/480 rules stop at `#main`, `#weatherOuter` and `#spaceForm`. Extend them — collapse `#mainMiddle` from `1fr 5fr 1fr 2fr 0.5fr` to stacked rows at 768px so `#weatherData` drops below `#weatherSection` as a 2×2 stat grid instead of clipping inside a 2fr column; revert the 480px `#weatherOuter` stack that strands `arrowLeft` above and `arrowRight` below the content, instead zeroing the `#inner1`/`#inner2` spacer rows (`upperLeft`/`lowerLeft`, `upperRight`/`lowerRight`) and narrowing the arrow columns so the pair flanks the content; and replace `#outerContainer`'s fixed `height: 110vh` and `10vh 70px 1fr 20px 35px 90px 20vh` track list with `min-height` plus auto rows so stacked mobile content grows the box rather than pushing `#spaceForm` and `#footer` out of it. Also set `#inputInner` to `font-size: 16px` — it's unset today and inherits ~13px, which triggers iOS Safari auto-zoom on focus. Reuse the existing 768 and 480 breakpoints; don't introduce new ones.
  - File: `WeatherApp_main/src/style.css`, `WeatherApp_main/src/indexChanges.js`, `WeatherApp_main/src/index.js`
  - Completed: 2026-07-15
  <!-- id: b7e829c7-2ab4-4b76-9ab6-075d3a30fa99 -->

- [x] **[HIGH]** Fix weather theme not repainting the sky and mobile layout regressions
  - Type: bug
  - Description: The shipped per-weather theme applies its accent but never its background — on a sunny day `#dateMain` and `#weatherType` take the warm accent rule while the sky stays the fallback blue. Cause is specificity: the `.weather-{condition}` class (0,1,0) loses to the base `#outerContainer` rule (1,0,0) carrying the fallback gradient, while the accent rules win because `.weather-sunny #dateMain` (1,1,0) beats `#dateMain` (1,0,0). Qualify the background rules as `#outerContainer.weather-sunny` etc., and move the fallback gradient off the base `#outerContainer` rule onto a `.weather-default` class applied in the same `adjustWeather()` branch so all eight states compete at equal specificity — also confirm the gradient isn't declared on `body` while the class lands on `#outerContainer`, which produces the identical symptom. Five layout defects surfaced with it, all now visible because the page has a background for the first time: (1) `offLeftArrow()`/`offRightArrow()` blank the arrows with `leftArrowIcon.src = ''`, which renders a broken-image placeholder box — pre-existing but invisible white-on-white — so toggle `visibility: hidden`/`visible` alongside the existing `pointerEvents` juggling and stop touching `src` after `index.js` sets it; (2) `#mainMiddle` overflows the viewport right edge because `#weatherIcon`'s hard `width: 140px` and `#tempUpper`'s 36px exceed the min-content of their `fr` tracks, which default to `min-width: auto` and refuse to shrink — change the `#mainMiddle` and `#weatherUpper` track lists to `minmax(0, Nfr)` and add mobile sizes (`#weatherIcon` ~88px, `#tempUpper` ~28px) at 480px; (3) the 480px arrow-column narrowing from the approved mockup never landed — `#weatherOuter` still splits `1fr 4fr 1fr`, giving ~63px gutters to 65px arrows, so they clip against the viewport edges; set `#weatherOuter` to `44px 1fr 44px` at 480px, shrink `#arrowLeft`/`#arrowRight` to ~32px, and zero the `#inner1`/`#inner2` spacer rows (`upperLeft`/`lowerLeft`, `upperRight`/`lowerRight`) so the pair sits at the date's vertical centre; (4) `#dateMain` wraps to two lines at 26px in a narrow column and stops reading as a rule — drop to ~18px with `white-space: nowrap` at 480px; (5) `#mainMiddle` has a border and `border-radius: 8px` but no `background-color`, so its rounded top frames the page gradient and the white begins with a hard square edge where `#weatherUpper` starts — move the white onto `#mainMiddle` and let `#weatherUpper` inherit. Excess vertical gaps between the date and the card come from the `1fr`/`3fr`/`2fr` spacer rows in `#mainTop` and `#innerMain` absorbing the height freed by the `min-height` change; zero those tracks at 480px too. Reuse the existing 768 and 480 breakpoints.
  - File: `WeatherApp_main/src/style.css`, `WeatherApp_main/src/indexChanges.js`
  - Completed: 2026-07-15
  <!-- id: d9cd7002-26f4-47d2-b584-2a6d9e567763 -->

- [x] **[HIGH]** Fix event listeners multiplying on every forecast fetch
  - Type: bug
  - Description: `changeWeatherInfo()` registers its two arrow listeners and its submit listener every time it runs, and `fulfillPromise()` in `logic.js` calls it on every resolved API response — so each search doubles the handler count. Initial load leaves one submit listener; the first click fires one `futureAPICalls`, resolves, re-runs `changeWeatherInfo`, and leaves two. The next click fires two, resolves twice, and leaves four — then eight, sixteen. That means N real fetches to weatherapi per click and N× the key's quota. The arrows break the same way: each generation closes over its own `let dayCounter = 1`, so every stacked handler increments a private counter and calls its own `dayNCall()`, and the day you end up seeing is whichever handler ran last. Generations created later start at 1 while older ones sit at 2 or 3, so they diverge immediately and the ‹ › pair stops tracking the day on screen. (`alldays` is fine — it's a shared reference, so the data is current; only the counter is per-closure.) Expected: exactly one listener per element for the life of the page, one fetch per submit click. Fix by splitting `changeWeatherInfo` into a pure render path — `renderDay(dayArray, n)` — that `fulfillPromise` can call freely, plus a `wireEvents()` that runs once from `index.js` at bootstrap after the DOM is appended, and hoist `dayCounter` to module scope so every handler reads one source of truth. Keep the initial `day1Call()` and the `offLeftArrow()` reset on the render path, not the wiring path — they must re-run for each new location. This blocks the swipe work: touch listeners added to `changeWeatherInfo` as it stands would multiply identically. While in the file, also fix `indexChanges.js:24-25`, where the lookups are crossed (`weatherType` holds `#weatherLocation` and `weatherLocation` holds `#weatherType`) — it works today only because both assignments are inverted to match, but it's why the accent rule underlines the city instead of the condition, and any future edit that trusts the variable names will write text into the wrong element.
  - File: `WeatherApp_main/src/indexChanges.js`, `WeatherApp_main/src/logic.js`, `WeatherApp_main/src/index.js`
  - Completed: 2026-07-15
  <!-- id: 19417fcd-1fd7-4b5a-b6a3-b5ee38aa1684 -->

- [x] **[MEDIUM]** Add swipe navigation, frosted card and animated weather sky
  - Type: feature
  - Description: Depends on the listener fix above — don't start until `changeWeatherInfo` wires once. Four parts. (1) Swipe: below 480px, replace the arrow pair with `touchstart`/`touchmove`/`touchend` on `#mainMiddle`, committing a day change past ~50px of horizontal travel and only when `|dx| > |dy|` so vertical page scroll isn't hijacked. Add a three-dot indicator under the card, active dot filled with the current weather accent — swipe with no affordance turns a 3-day forecast into a 1-day forecast. Arrows stay above 480px and desktop does not get dots; hide the arrow `<img>` pair at 480px with `visibility`, never `src = ''`, which renders a broken-image placeholder. Route swipe through the same day-change path as the arrows so both drive one counter. (2) Frosted card: `#mainMiddle` takes `background: rgba(255,255,255,0.58)` with `backdrop-filter: blur(11px) saturate(1.15)` and an `rgba(255,255,255,0.75)` border; `#weatherUpper` and `#weatherData` must drop their `background-color: white` and inherit, or they punch opaque holes through the frost. The card stays light at every condition — the icons load as `<img src>` and CSS can't reach inside them to recolor. (3) Animated sky: new `sky.js` mounting a `<canvas>` behind `#outerContainer`, running a per-condition particle field keyed off the category `adjustWeather()` already resolves. Vanilla only, no deps. Must include `prefers-reduced-motion: reduce` (render one static frame, then stop), a `visibilitychange` pause for the `rAF` loop, `resize` re-seeding, and a `devicePixelRatio` clamp at 2 — this loop runs on every device that loads the page, so the pause and reduced-motion paths aren't optional. (4) Centering and alignment at 480px: `#outerContainer` gets `min-height: 100dvh` and `align-content: center`, and the `1fr 3fr 2fr` tracks in `#mainTop` plus `1fr 4fr 0.5fr` in `#innerMain` collapse to `auto` so they stop absorbing the slack that floats the card ~160pt below the top edge. Use `dvh`, not `vh` — on iOS Safari `vh` resolves to the tall viewport, which is what parks the bottom of the layout under the URL bar; centering against `100vh` would make that worse. Match `#spaceForm`'s horizontal inset to `#mainMiddle`'s so Submit stops rendering wider than the card above it. Drop `#header` and `#footer` from `10vh`/`20vh` to content height and remove the accent fill from the footer. Finally, unify the stat scale inside `#weatherData` — values are 20/24/24/15px, labels 12/11/11/11px, icons 35/35/35/30px and icon padding 5/5/5/8px across four sibling blocks; pick one of each. Reuse the existing 768 and 480 breakpoints.
  - File: `WeatherApp_main/src/sky.js`, `WeatherApp_main/src/style.css`, `WeatherApp_main/src/indexChanges.js`, `WeatherApp_main/src/index.js`
  - Completed: 2026-07-15
  <!-- id: 2128a386-aaf8-4d16-83b0-baaf6c848806 -->

- [ ] **[MEDIUM]** Split sky.js into per-condition renderers and give sunny a clear-sky branch
  - Type: feature
  - Description: `sky.js` currently runs one particle emitter for every condition, so a clear day renders drifting motes — visible on screen right now at 32.6 °C with a 2% chance of rain. Sunny gets its own branch, and that branch renders nothing: the canvas clears, the `rAF` loop cancels, and the sky is the gradient alone. Stillness is the point rather than a saving — motion only reads as weather if it's absent when there's no weather, and a rain field that runs on sunny days is ambient decoration that happens to also appear during rain. The wider change is that the emitter only genuinely fits three of the eight branches, so restructure around renderer kind instead of one emitter with per-condition parameters; otherwise every branch added from here is another knob on a rain machine. Dispatch off the same category `adjustWeather()` already resolves from the `i`-flagged regexes, so the icon, the `.weather-*` theme class and the sky renderer all derive from one resolution — do not introduce a second classifier that can disagree with the first.
  - Behavior:
    1. Kind `particles` — rain, snow, hail. Canvas plus `rAF`. Differs per condition in drop shape, speed, drift and density: streaks falling fast for rain, slow laterally-drifting flakes for snow, short fast pellets for hail.
    2. Kind `layer` — fog, overcast, partly. CSS drift layers on `#outerContainer`, no canvas involvement at all. A haze band for fog, a slow cloud mass for overcast, a few slow clouds for partly.
    3. Kind `none` — sunny and the neutral fallback. No renderer. Cancel `rAF`, `clearRect` once, leave the canvas idle.
  - Acceptance criteria:
    - Five of the eight branches (sunny, fallback, fog, overcast, partly) never start the `rAF` loop.
    - Switching to a `none`-kind condition cancels the loop and clears the canvas rather than running it with zero particles; switching away re-seeds cleanly. Verify by searching a sunny location, then a rainy one, then back.
    - The existing `prefers-reduced-motion` static-frame path, `visibilitychange` pause, `resize` re-seed and `devicePixelRatio` clamp survive the refactor and apply per kind, not just to `particles`.
    - No new dependencies — vanilla canvas and CSS only.
  - Out of scope: the white strips above and below the sky (`body` has no `background-color`, so the canvas outside `#outerContainer`'s box falls back to white) — that's a separate bug entry, and it needs the gradient moved to `document.documentElement` plus `position: fixed` on the canvas.
  - File: `WeatherApp_main/src/sky.js`, `WeatherApp_main/src/style.css`, `WeatherApp_main/src/indexChanges.js`
  - Completed: YYYY-MM-DD (PR #<number>)
  <!-- id: 615a0f08-5f21-405e-9153-4a719ff8d928 -->
