# Dependencies — never commit installed packages. A committed node_modules
# tree (including gh-pages' cache dir, which contains a nested .git) was
# misread as an undeclared submodule and broke `actions/checkout`.
node_modules/
